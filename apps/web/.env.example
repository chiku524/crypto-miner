# Cloudflare (set in Workers dashboard or .dev.vars for local)
# Auth uses D1 + KV. R2 is available for storage (e.g. network logos).
# No env vars needed for bindings - they come from wrangler.toml.

# Optional: App URL for server-side fetch (e.g. session check from API)
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# Optional: Desktop app download URLs (for /download page). Set to full installer URLs, e.g. GitHub Releases.
# NEXT_PUBLIC_DESKTOP_DOWNLOAD_WIN=
# NEXT_PUBLIC_DESKTOP_DOWNLOAD_MAC=
# NEXT_PUBLIC_DESKTOP_DOWNLOAD_LINUX=
