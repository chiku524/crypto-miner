export const metadata = {
  title: 'Download desktop app — VibeMiner',
  description: 'Download VibeMiner for Windows, macOS, or Linux. Same mining dashboard, native app.',
};

export default function DownloadLayout({ children }: { children: React.ReactNode }) {
  return children;
}
