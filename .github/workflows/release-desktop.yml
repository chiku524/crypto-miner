# Build VibeMiner desktop app for Windows, macOS, and Linux on tag push (v*),
# then create a GitHub Release with the installers attached.
#
# Optional: Add secrets for code signing (Windows) and notarization (macOS)
# for a more trustworthy install. See docs/DESKTOP_APP.md.

name: Release desktop app

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build and release (e.g. v1.0.8). Use when the tag exists but the release was never created.'
        required: true
        type: string

permissions:
  contents: write

env:
  RELEASE_REF: ${{ github.event_name == 'workflow_dispatch' && format('refs/tags/{0}', inputs.tag) || github.ref }}
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_REF }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Set version from tag
        run: node -e "const fs=require('fs'); const p=require('./apps/desktop/package.json'); p.version=process.env.RELEASE_TAG.replace(/^v/i,''); fs.writeFileSync('./apps/desktop/package.json', JSON.stringify(p,null,2));"
        env:
          RELEASE_TAG: ${{ env.RELEASE_TAG }}

      - name: Install dependencies
        run: npm ci

      - name: Install desktop app dependencies
        run: npm install
        working-directory: apps/desktop

      - name: Generate app icons
        run: npm run generate-icons
        working-directory: apps/desktop

      # Set repo variable WIN_CODE_SIGNING_ENABLED = true when secrets are configured (secrets can't be used in if)
      - name: Prepare Windows code signing cert
        if: ${{ vars.WIN_CODE_SIGNING_ENABLED == 'true' }}
        run: |
          $certPath = "$env:GITHUB_WORKSPACE\apps\desktop\cert.pfx"
          [IO.File]::WriteAllBytes($certPath, [Convert]::FromBase64String($env:WIN_CODE_SIGNING_CERT_BASE64))
          "CSC_LINK=$certPath" | Out-File -FilePath $env:GITHUB_ENV -Append
          node -e "const fs=require('fs'); const p=require('./apps/desktop/package.json'); p.build.win.signAndEditExecutable=true; fs.writeFileSync('./apps/desktop/package.json', JSON.stringify(p,null,2));"
        env:
          WIN_CODE_SIGNING_CERT_BASE64: ${{ secrets.WIN_CODE_SIGNING_CERT_BASE64 }}

      - name: Build Windows installer
        run: npx electron-builder --win
        working-directory: apps/desktop
        env:
          CSC_KEY_PASSWORD: ${{ secrets.WIN_CODE_SIGNING_PASSWORD }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-windows
          path: |
            apps/desktop/dist/*.exe
            apps/desktop/dist/*.yml
            apps/desktop/dist/*.blockmap

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_REF }}

      - name: Set version from tag
        run: node -e "const fs=require('fs'); const p=require('./apps/desktop/package.json'); p.version=process.env.RELEASE_TAG.replace(/^v/i,''); fs.writeFileSync('./apps/desktop/package.json', JSON.stringify(p,null,2));"
        env:
          RELEASE_TAG: ${{ env.RELEASE_TAG }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install desktop app dependencies
        run: npm install
        working-directory: apps/desktop

      - name: Generate app icons
        run: npm run generate-icons
        working-directory: apps/desktop

      - name: Build macOS installer
        run: npx electron-builder --mac
        working-directory: apps/desktop
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-macos
          path: |
            apps/desktop/dist/*.dmg
            apps/desktop/dist/*.yml
            apps/desktop/dist/*.blockmap

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_REF }}

      - name: Set version from tag
        run: node -e "const fs=require('fs'); const p=require('./apps/desktop/package.json'); p.version=process.env.RELEASE_TAG.replace(/^v/i,''); fs.writeFileSync('./apps/desktop/package.json', JSON.stringify(p,null,2));"
        env:
          RELEASE_TAG: ${{ env.RELEASE_TAG }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install desktop app dependencies
        run: npm install
        working-directory: apps/desktop

      - name: Generate app icons
        run: npm run generate-icons
        working-directory: apps/desktop

      - name: Build Linux AppImage
        run: npx electron-builder --linux
        working-directory: apps/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-linux
          path: |
            apps/desktop/dist/*.AppImage
            apps/desktop/dist/*.yml

  release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: desktop-*
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: VibeMiner desktop ${{ env.RELEASE_TAG }}
          body: |
            ## Desktop installers
            Download the installer for your platform below. Same app as the web—runs in its own window. Built and published by [nico.builds](https://nico.builds).
            - **Windows**: run the `.exe` installer (publisher: nico.builds)
            - **macOS**: open the `.dmg` and drag VibeMiner to Applications
            - **Linux**: make the `.AppImage` executable and run it
          files: artifacts/*
          draft: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
